export const HELLO_WORLD = `
vec4 main(vec2 pos) {
  // output color is vector of red, green, blue,
  // and 4th valuenot used in this example
  // this just renders a straight pink solid
  // go ahead and play with the values below
  return vec4(1, 0.5, 0.8, 1);
}`;
